import React from 'react';

function TestQuestionsSlider(){
    // Индекс текущего вопроса
    // В начале показываем первый вопрос (индекс 0)
    const [index, setCurrentIndex] = React.useState(0);

    const [isDescription, setIsDescription] = React.useState('');


}
